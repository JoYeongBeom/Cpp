sqlite3 sj.db

학생 성적 테이블 생성
CREATE TABLE IF NOT EXISTS scores (
    student_id INTEGER PRIMARY KEY,
    subject1 INTEGER,
    subject2 INTEGER
);

학생 성적 삽입
INSERT INTO scores (student_id, subject1, subject2) VALUES
    (1, 80, 75),
    (2, 65, 70),
    (3, 90, 85);
